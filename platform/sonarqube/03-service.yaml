apiVersion: v1
kind: Service
metadata:
  name: sonarqube
  namespace: satmonitor-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sonarqube
  ports:
    - name: http
      port: 9000
      targetPort: 9000
