apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: satmonitor-dev

# Shared infrastructure only — each service is its own ArgoCD Application
# pointing directly to its component directory (backend-login/, job-service/, frontend/)
resources:
  - namespace.yaml
  - postgres/
  - ingress/
