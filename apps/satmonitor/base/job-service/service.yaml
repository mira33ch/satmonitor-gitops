apiVersion: v1
kind: Service
metadata:
  name: job-service
  namespace: satmonitor-dev
  labels:
    app.kubernetes.io/name: job-service
    app.kubernetes.io/part-of: satmonitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: job-service
  ports:
    - name: http
      port: 8088
      targetPort: http
      protocol: TCP
