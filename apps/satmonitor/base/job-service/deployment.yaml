apiVersion: apps/v1
kind: Deployment
metadata:
  name: job-service
  namespace: satmonitor-dev
  labels:
    app.kubernetes.io/name: job-service
    app.kubernetes.io/part-of: satmonitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: job-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: job-service
        app.kubernetes.io/part-of: satmonitor
    spec:
      containers:
        - name: job-service
          image: mariem360/job-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8088
              name: http
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: k8s
            - name: SERVER_PORT
              value: "8088"
          envFrom:
            - configMapRef:
                name: job-service-config
            - secretRef:
                name: job-service-secrets
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 90
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 200m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
