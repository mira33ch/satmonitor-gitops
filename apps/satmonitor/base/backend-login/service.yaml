apiVersion: v1
kind: Service
metadata:
  name: login-service
  namespace: satmonitor-dev
  labels:
    app.kubernetes.io/name: login-service
    app.kubernetes.io/part-of: satmonitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: login-service
  ports:
    - name: http
      port: 8087
      targetPort: http
      protocol: TCP
