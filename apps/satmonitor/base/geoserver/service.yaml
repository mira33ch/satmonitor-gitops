apiVersion: v1
kind: Service
metadata:
  name: satmonitor-geoserver
  namespace: satmonitor-dev
spec:
  type: ClusterIP
  selector:
    app: satmonitor-geoserver
  ports:
    - port: 8080
      targetPort: 8080