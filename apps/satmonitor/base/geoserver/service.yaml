apiVersion: v1
kind: Service
metadata:
  name: geoserver
  namespace: satmonitor-dev
  labels:
    app.kubernetes.io/name: geoserver
    app.kubernetes.io/part-of: satmonitor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: geoserver
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
