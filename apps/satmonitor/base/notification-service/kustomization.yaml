apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: satmonitor-dev

resources:
  - configmap.yaml
  # TODO: seal with kubeseal (see backend-login/job-service) and switch this
  # to sealed-secrets.yaml once the cluster's sealed-secrets public cert is
  # available.
  - secrets.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: mariem360/notification-service
    newTag: 4827ca2
