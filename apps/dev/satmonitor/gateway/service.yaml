apiVersion: v1
kind: Service
metadata:
  name: satmonitor-gateway
  labels:
    app.kubernetes.io/name: satmonitor-gateway
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: satmonitor-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
